{
	"name": "obsidian-plugin-monorepo",
	"version": "1.0.0",
	"description": "Obsidian plugin monorepo",
	"private": true,
	"scripts": {
		"dev": "pnpm --filter obsidian-doc-weaver dev",
		"dev:all": "pnpm -r --parallel dev",
		"build": "pnpm --filter obsidian-doc-weaver build",
		"build:all": "pnpm --filter '*' build",
		"lint": "pnpm --filter '*' lint",
		"test": "pnpm --filter '*' test",
		"clean": "rm -rf node_modules && pnpm -r exec -- rm -rf node_modules dist .turbo"
	},
	"keywords": [],
	"author": "",
	"license": "MIT",
	"engines": {
		"node": ">=16.0.0",
		"pnpm": ">=7.0.0"
	},
	"devDependencies": {
		"turbo": "^1.10.0",
		"typescript": "5.8.3"
	},
	"resolutions": {
		"markdown-it": "^14.1.0",
		"@types/markdown-it": "^14.1.2"
	}
}
