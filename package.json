{
	"name": "obsidian-plugin-monorepo",
	"version": "1.0.0",
	"description": "Obsidian plugin monorepo",
	"private": true,
	"scripts": {
		"dev": "turbo run dev --filter=obsidian-doc-weaver",
		"dev:all": "turbo run dev",
		"build": "turbo run build --filter=obsidian-doc-weaver",
		"build:all": "turbo run build",
		"lint": "pnpm --filter '*' lint",
		"test": "pnpm --filter '*' test",
		"clean": "rm -rf node_modules && pnpm -r exec -- rm -rf node_modules dist .turbo"
	},
	"keywords": [],
	"author": "",
	"license": "MIT",
	"engines": {
		"node": ">=16.0.0",
		"pnpm": ">=7.0.0"
	},
	"devDependencies": {
		"turbo": "^1.10.0",
		"typescript": "5.8.3"
	},
	"resolutions": {
		"markdown-it": "^14.1.0",
		"@types/markdown-it": "^14.1.2"
	}
}
