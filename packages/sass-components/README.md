# SASS Components Library

这是一个为 Obsidian 工具包设计的 SASS 组件库，提供了一套现代化的 UI 组件样式。

## 特性

- 🎨 现代化的设计系统
- 🎯 专为 Obsidian 插件优化
- 📱 响应式设计
- 🎪 模块化的组件架构
- 🛠️ 易于定制和扩展

## 快速开始

### 安装依赖

```bash
pnpm install
```

### 构建样式

```bash
# 开发模式（监听文件变化）
pnpm run dev

# 构建生产版本
pnpm run build

# 构建展开版本（用于调试）
pnpm run build:expanded
```

### 查看示例

```bash
# 启动本地服务器查看示例
pnpm run serve
```

然后在浏览器中访问 `http://localhost:8080`

## 目录结构

```
packages/sass-components/
├── src/
│   ├── styles/          # SASS 源文件
│   │   ├── base/        # 基础样式
│   │   ├── components/  # 组件样式
│   │   ├── utils/       # 工具样式
│   │   └── index.scss   # 主入口文件
│   └── components/      # 组件文档和示例
├── dist/                # 编译后的 CSS 文件
├── examples/            # HTML 示例文件
└── package.json
```

## 组件

### 按钮组件
- 主要按钮
- 次要按钮
- 危险按钮
- 图标按钮

### 输入组件
- 文本输入框
- 文本域
- 选择框
- 复选框

### 布局组件
- 容器
- 网格系统
- 卡片
- 面板

### 反馈组件
- 提示框
- 加载状态
- 进度条
- 通知

## 定制

所有的设计标记都定义在 `src/styles/base/_variables.scss` 中，您可以通过修改这些变量来定制整个设计系统。

```scss
// 主色调
$primary-color: #6366f1;
$secondary-color: #8b5cf6;

// 字体
$font-family-base: 'Inter', sans-serif;
$font-size-base: 14px;

// 间距
$spacing-unit: 8px;
```

## 开发

1. 克隆项目并安装依赖
2. 运行 `pnpm run dev` 开始开发
3. 在 `examples/` 目录中查看样式效果
4. 修改 `src/styles/` 中的 SASS 文件

## 许可证

MIT License
